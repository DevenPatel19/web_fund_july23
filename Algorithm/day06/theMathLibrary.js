// The Math Library
// Predict what each of the following will return. Can we predict the variable random?

var floor = Math.floor(1.8); //1
var ceiling = Math.ceil( Math.PI ); //4
var random = Math.random();  0-0.999999999999
    
console.log(floor);
console.log(ceiling);
console.log(random);